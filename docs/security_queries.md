# Security Queries

Placeholder documentation for SQL query templates in `sql/`.

Current milestone: **Milestone 1 — Repository Foundation and Security Engineering Scaffold**.

SQL files are skeleton templates only. Milestone 4 will implement collectors that read from `sql/` and execute queries through the mocked and tested Snowflake client wrapper.

Each query should document:

- Purpose
- Snowflake source view or command
- Required permissions
- Expected output columns
- Related controls
