-- Placeholder: collect object and role grants assigned to roles.
-- Future source candidates: SHOW GRANTS TO ROLE, SNOWFLAKE.ACCOUNT_USAGE.GRANTS_TO_ROLES.
SELECT
  NULL AS privilege,
  NULL AS granted_on,
  NULL AS name,
  NULL AS grantee_name
WHERE 1 = 0;
