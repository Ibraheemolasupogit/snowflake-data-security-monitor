-- Placeholder: collect role grants assigned directly to users.
-- Future source candidates: SHOW GRANTS TO USER, SNOWFLAKE.ACCOUNT_USAGE.GRANTS_TO_USERS.
SELECT
  NULL AS role,
  NULL AS grantee_name,
  NULL AS granted_by,
  NULL AS created_on
WHERE 1 = 0;
