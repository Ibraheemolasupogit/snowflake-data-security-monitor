-- Placeholder: collect recent login history.
-- Future filter should use config.collection.login_history_days.
SELECT
  NULL AS user_name,
  NULL AS event_timestamp,
  NULL AS is_success
WHERE 1 = 0;
