-- Placeholder: collect query history metadata for future context.
-- Avoid collecting sensitive query text unless explicitly needed and approved.
SELECT
  NULL AS user_name,
  NULL AS role_name,
  NULL AS start_time
WHERE 1 = 0;
