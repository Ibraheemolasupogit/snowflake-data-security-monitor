-- Placeholder: collect Snowflake users for security posture checks.
-- Future source candidates: SHOW USERS, SNOWFLAKE.ACCOUNT_USAGE.USERS.
SELECT
  NULL AS name,
  NULL AS disabled,
  NULL AS last_success_login
WHERE 1 = 0;
